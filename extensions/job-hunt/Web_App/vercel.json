{
  "framework": "vite",
  "installCommand": "if [ -f package.json ]; then npm install; else echo 'No package.json found, skipping install'; fi",
  "buildCommand": "if [ -f package.json ]; then npm run build; else echo 'No package.json found, using checked-in dist output'; fi",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}